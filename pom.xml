<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.piggymetrics</groupId>
	<artifactId>piggymetrics</artifactId>
	<version>1.0-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>piggymetrics</name>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.3.5</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<spring-cloud.version>2023.0.3</spring-cloud.version>
		<java.version>21</java.version>
		<testcontainers.version>1.20.6</testcontainers.version>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>${testcontainers.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<!-- Override the JNA pulled transitively by Testcontainers/docker-java
			     (4.5.1, x86_64/i386 only) with a build that ships an arm64 native,
			     required to run Testcontainers on Apple Silicon under a JDK 8 JVM. -->
			<dependency>
				<groupId>net.java.dev.jna</groupId>
				<artifactId>jna</artifactId>
				<version>5.13.0</version>
			</dependency>
		</dependencies>
	</dependencyManagement>
	
	<modules>
		<module>config</module>
		<module>registry</module>
		<module>account-service</module>
		<module>statistics-service</module>
		<module>notification-service</module>
		<!--
		  Phase 3 quarantine: these modules depend on Netflix/OAuth2 artifacts that
		  were removed in Spring Cloud 2023.x / Spring Security 6 and cannot compile
		  on the new platform. They are excluded from the JDK 21 reactor and stay on
		  the legacy code until their owning phase:
		    - gateway                -> Netflix Zuul removed; rewritten on Spring Cloud Gateway in Phase 4
		    - monitoring             -> Hystrix Dashboard removed; deleted in Phase 4
		    - turbine-stream-service -> Turbine Stream removed; deleted in Phase 4
		    - auth-service           -> spring-security-oauth2 authorization server removed; rewritten in Phase 5
		<module>gateway</module>
		<module>monitoring</module>
		<module>turbine-stream-service</module>
		<module>auth-service</module>
		-->
	</modules>

</project>
